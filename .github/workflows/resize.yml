name: Resize Images

on:
  push:
    paths:
      - 'flier/**'

jobs:
  resize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Resize to 800px width
        uses: sharadcodes/img-resizer@v1.1
        with:
          image-folder: 'images'
          new-resolution: '1444x'
          output-folder: 'resized'
